app:
  build: .
  command: 'npm run dev'
  volumes:
    - '.:/app'
  environment:
    PORT: 8080
    VIRTUAL_HOST: 'media.*'
    FORCE_COLOR: 1
    AWS_ACCESS_KEY_ID:
    AWS_BUCKET:
    AWS_SECRET_ACCESS_KEY:
    S3_HOST:
    PUBLIC:
    FOLDER:
  ports:
    - '8080'
